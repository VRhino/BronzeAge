# Dependencies
node_modules/
.pnpm-store/

# Build output — sin anclar a la raíz: `cliente/` es un proyecto aparte con su propio `vite build`
dist/
build/
coverage/

# TypeScript
*.tsbuildinfo

# Vite
.vite/
.vite-cache/

# Environment files
.env
.env.*
!.env.example

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log

# Editors and OS
.vscode/
.idea/
.DS_Store
Thumbs.db
issues/

# Snapshots de partida de `npm run server` (src/server/persistenciaPartida.ts), directorio por defecto
/partidas

# Bundle del laboratorio visual (se regenera con `npm run lab`)
/lab/dist/

# Salidas de graphify — sin anclar a la raíz: escribe `graphify-out/` en el directorio
# desde el que se lanza Y una caché de extracción dentro de la carpeta escaneada.
# Todo se regenera con `/graphify <path>`; el grafo de `src/` pesa ~5 MB.
graphify-out/

Docs/Coordinacion/fixtures/*